%% Ejercicio 1
%%% Rotaciones son R = zRotation(phi)*yRotation(phi)*xRotation(phi)
%%% Cuando hay un eje que no tiene rotación lo podemos obviar porque la
%%% matriz resultante es la matriz identidad

R1 = zRotation(90)*yRotation(90) 

R2 = zRotation(90)*xRotation(90)

R3 = zRotation(30)*yRotation(90)*xRotation(45)




